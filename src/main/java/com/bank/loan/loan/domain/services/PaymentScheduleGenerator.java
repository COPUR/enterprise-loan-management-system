package com.bank.loan.loan.domain.services;

import com.bank.loan.loan.domain.loan.LoanAmount;
import com.bank.loan.loan.domain.loan.LoanTerm;
import com.bank.loan.loan.domain.loan.InterestRate;
import com.bank.loan.loan.domain.loan.PaymentSchedule;

public class PaymentScheduleGenerator {

    public static PaymentSchedule generate(LoanAmount loanAmount, LoanTerm loanTerm, InterestRate interestRate) {
        // Implementation details for generating a payment schedule
        return new PaymentSchedule();
    }
}
