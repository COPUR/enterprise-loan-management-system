pipeline {
    agent any
    options { timestamps() }

    stages {
        stage('Validate Context Artifacts') {
            steps {
                sh '''
                  set -euo pipefail
                  test -f "services/bounded-contexts/loan-context/README.md"
                  test -f "api/openapi/loan-context.yaml"
                '''
            }
        }
    }
}
