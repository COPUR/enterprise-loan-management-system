pipeline {
    agent any
    options { timestamps() }

    stages {
        stage('Validate Context Artifacts') {
            steps {
                sh '''
                  set -euo pipefail
                  test -f "services/bounded-contexts/risk-context/README.md"
                  test -f "api/openapi/risk-context.yaml"
                '''
            }
        }
    }
}
